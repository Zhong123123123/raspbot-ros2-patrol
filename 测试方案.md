你现在在我的 Raspbot ROS2 巡检机器人项目中工作。

项目背景：
这是一个基于 Raspberry Pi 4B 8GB + Ubuntu 24.04 + ROS2 Jazzy 的巡检机器人系统。项目目标是形成“感知、决策、反馈、记录、运维”的完整巡检闭环，不依赖 SLAM/Nav2，而是通过纯 ROS2 节点架构实现可测试、可切换、可演示、可面试讲清的工程项目。

当前功能宣称如下，请你做一次全面系统功能运行实测，验证功能是否完整实现、接口是否一致、运行是否稳定，并生成详细测试报告：

系统分层：
- 感知层：人体检测 HOG / TF-SSD / YOLOv8n，超声波测距，红外避障，视觉循线
- 决策层：多轮确认，超时重试，移动路线，巡检状态机，障碍暂停
- 反馈层：LED，蜂鸣器，TTS 语音，Webhook 远程通知
- 记录层：SQLite 日志，截图证据链，Markdown 自动日报
- 运维层：systemd 自启，Web Dashboard，GPIO 清理
- 工程化：单元测试，3 检测后端可切换，YAML 参数化，一键 Launch

一句话定位：
Raspbot ROS2 巡检机器人 — 基于 Raspberry Pi 4B + Ubuntu 24.04 + ROS2 Jazzy，实现从感知、决策、反馈、记录到运维的完整巡检闭环；不依赖 SLAM/Nav2，纯 ROS2 节点架构，代码可测试可切换，面试可逐层讲清。

你的任务：
做一次“全面系统功能运行实测 + 验收报告”，不要优先开发新功能，不要重构主线代码。你要先确认功能是否真的存在、能否编译、能否启动、能否互相联动、能否留下证据。如果发现缺失或不一致，记录问题；只有在非常小的范围内可以修复明显的路径、launch、参数、topic 名称、脚本权限等集成问题。不要做大规模改造。

重要安全要求：
1. 任何会让小车运动的测试，第一次必须架空轮子或抬起小车测试。
2. 落地测试前必须确认 /cmd_vel 能归零，节点退出时能停车。
3. 移动巡逻测试速度必须保守，linear_x 不超过 0.08，angular_z 不超过 0.35。
4. 不要长时间高速运行电机。
5. 不要修改 Nav2 / SLAM / map 相关内容，本项目不做这些。
6. 不要擅自改底层硬件控制逻辑。
7. 如果硬件不在线或某传感器异常，不要强行继续危险测试，记录为 blocked 或 failed。

请按下面流程执行。

============================================================
一、项目静态盘点
============================================================

请先扫描项目结构，确认以下内容是否存在，并记录路径：

1. ROS2 包：
   - raspbot_base
   - raspbot_vision
   - raspbot_bringup
   - 其他相关包

2. 核心节点：
   - base_driver_node
   - gimbal_servo_node
   - ultrasonic_range_node
   - infrared / ir obstacle 相关节点
   - line_follow_node
   - person_detect_node
   - patrol_scheduler_node
   - patrol_behavior_node
   - patrol_logger_node
   - route_patrol_node
   - voice_announce_node / tts 相关节点
   - remote_notify_node / webhook 相关节点
   - web_dashboard_node
   - status_led_node
   - buzzer_node

3. Launch 文件：
   - base_bringup.launch.py
   - patrol_detection.launch.py
   - patrol_full.launch.py
   - patrol_full_yolov8.launch.py
   - route_patrol.launch.py
   - route_patrol_only.launch.py，如存在
   - dashboard 相关 launch，如存在

4. 配置文件：
   - person_detect.yaml
   - person_detect_yolov8.yaml
   - patrol_behavior.yaml
   - patrol_scheduler.yaml
   - patrol_logger.yaml
   - route_patrol.yaml
   - remote_notify.yaml
   - voice_announce.yaml
   - 其他 YAML 参数文件
   - （注：web_dashboard_node 无独立 YAML，参数直接在代码中声明）

5. 工具脚本（均在 ros2_ws/ 根目录或通过 python3 -m 调用）：
   - query_patrol_results.py（ros2_ws 根目录）
   - check_patrol_results.sh（ros2_ws 根目录）
   - cleanup_gpio.sh（ros2_ws 根目录）
   - generate_patrol_report.py（通过 python3 -m raspbot_vision.generate_patrol_report）
   - benchmark_yolov8.py（通过 python3 -m raspbot_vision.benchmark_yolov8）
   - compare_person_detectors（ros2 run）
   - download_yolov8_model.sh（ros2_ws 根目录）
   - patrol_delivery_check.sh（ros2_ws 根目录）
   - scripts/patrol-systemd-install.sh（systemd 安装脚本）

6. systemd：
   - patrol.service（由 scripts/patrol-systemd-install.sh 动态生成并安装）
   - 使用 sudo bash scripts/patrol-systemd-install.sh 安装
   - 通过 systemctl start/stop patrol 管理

7. 测试：
   - pytest / unittest 测试文件
   - 当前测试总数
   - 是否真的达到“99 个单元测试”
   - 测试覆盖哪些模块

请输出一份功能清单表，格式如下：

| 功能 | 文件/节点/脚本 | 是否存在 | 是否已接入 launch | 备注 |
|---|---|---|---|---|

如果某个功能宣称存在但找不到实现，不要脑补，标记为 MISSING。

============================================================
二、环境与编译测试
============================================================

执行并记录：

```bash
cd ~/ros2_ws
source /opt/ros/jazzy/setup.bash
./build.sh                          # 项目专用编译脚本（含 ament_python 修复）
source install/setup.bash
```

检查：
```bash
ros2 pkg list | grep raspbot
# ros2 node list 和 ros2 topic list 需要 launch 启动后才能用
```

运行测试：
```bash
source install/setup.bash
cd ~/ros2_ws
python3 -m pytest src/raspbot_vision/test/ -q
```

请记录：
- build 是否通过
- test 是否通过
- 实际测试数量是多少
- 失败测试名称
- 失败原因
- 是否影响主线功能

如果只有小问题，比如脚本无执行权限、路径引用错误、launch 参数文件路径错误，可以做最小修复，并记录修复内容。

============================================================
三、接口一致性检查
============================================================

请不要直接假设 topic 类型，必须实际查：

ros2 topic list -t
ros2 interface show <message_type>

重点确认以下 topic 是否存在、类型是否符合使用方：

- cmd_vel（geometry_msgs/Twist）
- safety_cmd_vel（geometry_msgs/Twist），如存在
- gimbal_cmd（geometry_msgs/Vector3）
- person_detection/trigger（std_msgs/Bool）
- person_detection/result（std_msgs/String）
- person_detection/status（std_msgs/String）
- patrol/trigger（std_msgs/Bool，⚠️ 不是 String）
- patrol/final_result（std_msgs/String, JSON）
- patrol/confirmed_status（std_msgs/String, JSON）
- patrol/alert（std_msgs/Bool）
- route_patrol/status（std_msgs/String, JSON）
- ultrasonic/front（sensor_msgs/Range）
- status_led_cmd（std_msgs/ColorRGBA）
- buzzer_beep_ms（std_msgs/UInt16）
- Dashboard HTTP 端口 8080

检查所有订阅发布是否对得上：
- patrol_behavior_node 发布的是否正好是 person_detect_node 订阅的
- person_detect_node 发布的是否正好是 patrol_behavior_node 订阅的
- patrol_logger_node 是否能接收到 final_result / observations
- route_patrol_node 是否能触发 patrol_behavior_node
- remote_notify_node 是否订阅 confirmed_status 或 alert
- voice_announce_node 是否订阅 confirmed_status 或 alert
- web_dashboard_node 是否订阅核心状态 topic

输出：
| Topic | Type | Publisher | Subscriber | 状态 |
|---|---|---|---|---|

状态分为：
- OK
- NO_PUBLISHER
- NO_SUBSCRIBER
- TYPE_MISMATCH
- NOT_FOUND

============================================================
四、纯软件功能测试，不让小车运动
============================================================

先做不会让电机运动的测试。

1. 检查配置加载：
   - 所有 YAML 是否能被 launch 正确加载
   - 参数名是否和节点 declare_parameter 一致
   - 无无效参数或拼写错误

2. 检查 SQLite：
   - 数据库路径是否存在
   - patrol_events 表是否存在
   - patrol_observations 表是否存在
   - 字段是否包含：
     patrol_id
     started_at_utc
     finished_at_utc
     final_decision
     detected
     max_person_count
     max_confidence
     occupied_positions
     raw_image_path
     debug_image_path
     scan_position
     camera_backend
     error_msg

3. 检查查询脚本：
   ```bash
   python3 query_patrol_results.py --limit 10
   python3 query_patrol_results.py --mode final --limit 10
   python3 query_patrol_results.py --mode observations --limit 10
   python3 query_patrol_results.py --decision occupied
   python3 query_patrol_results.py --csv /tmp/patrol_results_test.csv
   ```

4. 检查日报脚本：
   ```bash
   python3 -m raspbot_vision.generate_patrol_report
   python3 -m raspbot_vision.generate_patrol_report -o /tmp/patrol_report_test.md
   ```
   确认是否生成 Markdown 报告，路径记录下来。

5. 检查 GPIO 清理脚本：
   只检查脚本存在、权限、帮助信息或 dry-run。
   不要在不确定的情况下破坏硬件状态。

6. 检查远程通知：
   如果配置了真实 Webhook，不要滥发多次。
   优先使用 dry-run / mock / test mode。
   检查远程通知节点能启动，收到 /patrol/alert 或 /patrol/confirmed_status 后能进入发送逻辑。
   如果没有 token 或 webhook_url，标记为 CONFIG_MISSING，不算代码失败。

7. 检查 TTS：
   检查 TTS 节点是否能启动。
   如果有 test 参数，播放一次短文本：
   “巡检系统测试”
   如果当前无音箱或环境不适合播放，记录为 BLOCKED_HARDWARE。

============================================================
五、硬件节点冒烟测试
============================================================

启动基础硬件 launch：

ros2 launch raspbot_bringup base_bringup.launch.py

检查：
ros2 node list
ros2 topic list -t

逐项测试：

1. 底盘控制：
   架空轮子后发布低速短指令：
   ```bash
   ros2 topic pub --once cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.03}, angular: {z: 0.0}}"
   # 然后立刻发布零速度
   ros2 topic pub --once cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.0}, angular: {z: 0.0}}"
   ```

   记录：
   - 轮子是否转动
   - 方向是否符合预期
   - 零速度后是否停止

2. 云台：
   发布左/中/右角度或调用现有 gimbal topic。
   记录云台是否能移动。
   不清楚消息格式时先查 topic type，不要乱发。

3. 超声波：
   ros2 topic echo /ultrasonic/front
   用手或物体靠近，观察距离是否变化。
   记录单位是否为 m，阈值是否合理。

4. 红外避障：
   如果有 topic，echo 观察是否变化。
   如果红外不稳定，记录，不要作为主线失败。

5. LED：
   发布状态灯指令，观察灯变化。
   不清楚消息格式时先查接口。

6. 蜂鸣器：
   发布短鸣 100ms 或 200ms。
   不要长鸣。
   记录是否有效。

7. 摄像头：
   检查 /dev/video* 和 /dev/v4l/by-id/ 路径。
   启动 person_detect_node。
   检查是否能打开相机、读取帧、保存图片。

============================================================
六、人体检测三后端测试
============================================================

测试 HOG / TF-SSD / YOLOv8n 三个后端是否可切换。

要求：
1. 不要只看代码存在，要实际启动。
2. 如果模型文件缺失，记录 MODEL_MISSING，不要乱下载大文件，除非已有脚本并且用户允许。
3. 每个后端至少做一次触发式检测。
4. 记录推理耗时、检测结果、截图路径、debug 图路径。

测试方式：
- HOG 后端 launch 或 params
- TF-SSD 后端 launch 或 params
- YOLOv8n ONNX 后端 launch 或 params

对每个后端记录：

| Backend | 是否能加载 | 是否能读相机 | 是否能触发检测 | 是否有 raw 图 | 是否有 debug 图 | 耗时 | 备注 |
|---|---|---|---|---|---|---|---|

如果 YOLOv8n ONNX 模型不存在：
- 检查 download_yolov8_model.sh 是否存在
- 检查 benchmark_yolov8.py 是否存在
- 标记为 MODEL_MISSING，而不是代码失败

============================================================
七、完整定点巡检闭环测试
============================================================

启动完整巡检：

ros2 launch raspbot_bringup patrol_full.launch.py

然后执行：

1. 检查节点全部起来：
   ros2 node list

2. 检查核心 topic：
   ros2 topic list -t

3. 手动触发一次巡检：
   根据实际 /patrol/trigger 类型发布一次 trigger。

4. 观察：
   - 小车是否先停车
   - 云台是否左/中/右扫描
   - 每个角度是否触发人体检测
   - 是否发布 /person_detection/result
   - 是否发布 /patrol/final_result
   - 是否写入 SQLite
   - 是否保存 raw/debug 图片
   - 是否触发 LED / 蜂鸣器 / TTS / Webhook
   - Dashboard 是否看到状态变化

5. 查询数据库：
   sqlite3 ~/ros2_ws/data/patrol/patrol_events.db "select * from patrol_events order by id desc limit 3;"
   sqlite3 ~/ros2_ws/data/patrol/patrol_events.db "select * from patrol_observations order by id desc limit 9;"

6. 查询脚本：
   ```bash
   python3 query_patrol_results.py --limit 3
   python3 query_patrol_results.py --mode observations --limit 9
   ```

记录：
- patrol_id
- final_decision
- occupied_positions
- max_confidence
- raw_image_path
- debug_image_path
- 是否文件真实存在

============================================================
八、多轮确认与超时重试测试
============================================================

测试 patrol_behavior_node 的策略功能是否真正生效。

检查 YAML 参数：
- confirm_occupied_rounds
- confirm_empty_rounds
- occupied_hold_sec
- uncertain_retry_count
- detection_timeout_sec
- trigger window / anti-jitter 参数

实测：
1. 连续触发多轮巡检。
2. 观察 /patrol/confirmed_status。
3. 观察 /patrol/alert。
4. 检查 occupied 是否需要连续多轮才确认。
5. 检查 empty 是否需要连续多轮才确认。
6. 模拟检测超时或相机不可用时，是否进入 uncertain 或 retry。
7. occupied HOLD 结束后 alert 是否恢复 false。

输出：
| 测试项 | 预期 | 实际 | 通过 |
|---|---|---|---|

============================================================
九、轻量移动路线巡逻测试
============================================================

注意：第一次必须架空轮子测试。

检查 route_patrol.yaml：
- move / turn / wait / stop / patrol action 是否都配置正确
- 速度是否低于安全上限
- duration 是否短
- obstacle_stop_distance_m / obstacle_resume_distance_m 是否合理

启动：
ros2 launch raspbot_bringup route_patrol.launch.py
或：
ros2 launch raspbot_bringup patrol_full.launch.py
ros2 launch raspbot_bringup route_patrol_only.launch.py

观察：
ros2 topic echo /route_patrol/status
ros2 topic echo /cmd_vel
ros2 topic echo /patrol/trigger
ros2 topic echo /patrol/final_result

测试内容：
1. route_patrol_node 能否启动。
2. 是否能按 YAML 执行 move / turn / wait / stop。
3. 每个移动动作结束后是否连续发布零速度。
4. patrol 动作是否触发 /patrol/trigger。
5. 收到 /patrol/final_result 后是否继续下一步。
6. 路线结束后是否停车。
7. 节点 Ctrl+C 退出时是否停车。
8. 前方障碍低于阈值时是否暂停。
9. 障碍解除后是否继续。

如果障碍暂停不好实测，可以用 topic mock，但不要破坏真实超声波节点。

============================================================
十、Web Dashboard 测试
============================================================

启动 Dashboard 节点。

检查：
- HTTP 页面是否可访问
- MJPEG 是否正常
- WebSocket 是否正常
- 是否显示：
  - 人体检测状态
  - confirmed_decision
  - alert
  - route_patrol/status
  - 摄像头图像
  - 虚拟摇杆，如存在
  - 日志或最近结果，如存在

记录访问地址、端口、页面是否正常。

如果需要截图，保存到：
~/ros2_ws/data/system_test/dashboard_YYYYMMDD_HHMMSS.png

============================================================
十一、systemd 自启测试
============================================================

先运行安装脚本（由它动态生成 patrol.service）：

```bash
sudo bash scripts/patrol-systemd-install.sh
```

然后检查：

1. 静态检查：
   ```bash
   systemctl cat patrol.service
   ```

2. 检查：
   - WorkingDirectory 是否正确（~/ros2_ws）
   - 是否 source /opt/ros/jazzy/setup.bash
   - 是否 source ~/ros2_ws/install/setup.bash
   - ExecStart 是否启动 patrol_full.launch.py
   - Restart=on-failure 是否配置
   - User 是否正确
   - 日志是否能通过 journalctl 查看

3. 手动启动测试：
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl start patrol.service
   systemctl status patrol.service
   journalctl -u patrol.service -n 100 --no-pager
   ```

4. 停止：
   ```bash
   sudo systemctl stop patrol.service
   ```

除非用户明确要求，不要执行 enable 开机自启。
如果已经 enable，记录当前状态。

============================================================
十二、异常场景测试
============================================================

尽量覆盖：

1. 摄像头断开 / 路径错误：
   - person_detect_node 是否崩溃
   - 是否重试
   - 是否记录 error_msg
   - 巡检结果是否为 uncertain

2. 模型文件缺失：
   - YOLO / TF-SSD 模型缺失时是否 fallback 到 HOG
   - 是否有清晰日志

3. SQLite 路径不存在：
   - 是否自动创建目录
   - 是否报清晰错误

4. GPIO busy：
   - cleanup_gpio.sh 是否可用于恢复
   - 是否有文档说明

5. Webhook 未配置：
   - 是否不影响主巡检
   - 是否只记录 warning

6. TTS 不可用：
   - 是否不影响主巡检
   - 是否只记录 warning

7. route_patrol 中途 Ctrl+C：
   - 是否发布零速度
   - 小车是否停止

============================================================
十三、稳定性测试
============================================================

在功能冒烟测试通过后，再做稳定性测试。

第一阶段：
连续运行 10 分钟，观察：
- 节点是否退出
- 内存是否明显上涨
- CPU 是否过高
- 相机是否断流
- SQLite 是否持续正常
- Dashboard 是否断开

第二阶段：
连续触发 20 轮巡检，记录：
- 成功次数
- failed 次数
- uncertain 次数
- occupied / empty 次数
- 平均单轮耗时
- 最大单轮耗时
- 是否有漏写日志
- 是否每轮有 observations
- 是否每轮有截图

第三阶段：
连续运行 30 分钟：
- 如果硬件安全条件允许再执行
- 记录日志
- 最终导出测试摘要

建议记录命令输出到：
~/ros2_ws/data/system_test/logs/

可以用 tee 保存：
ros2 launch ... 2>&1 | tee ~/ros2_ws/data/system_test/logs/patrol_full_YYYYMMDD_HHMMSS.log

============================================================
十四、最终交付报告
============================================================

请生成：

docs/SYSTEM_TEST_REPORT.md

报告必须包含：

1. 测试日期、设备环境
   - Raspberry Pi 型号
   - Ubuntu 版本
   - ROS2 版本
   - Python 版本
   - 工作区路径
   - Git commit hash

2. 功能验收总览表：

| 模块 | 功能 | 状态 | 证据 | 问题 |
|---|---|---|---|---|

状态使用：
- PASS：功能存在且实测通过
- PARTIAL：部分实现或部分通过
- FAIL：实现存在但实测失败
- MISSING：宣称有但找不到实现
- BLOCKED：硬件/配置缺失导致无法测
- NOT_TESTED：本轮未测试

3. 核心运行证据：
   - build 结果
   - test 结果
   - ros2 node list
   - ros2 topic list -t
   - patrol_full.launch.py 启动日志
   - 一次完整巡检 patrol_id
   - SQLite 查询结果
   - raw/debug 图片路径
   - Dashboard 访问结果
   - route_patrol/status 示例
   - systemd status 示例

4. 问题清单：
| ID | 严重级别 | 问题 | 复现方式 | 影响 | 建议修复 |
|---|---|---|---|---|---|

严重级别：
- P0：会导致主系统无法启动或小车危险运动
- P1：主线功能失败
- P2：支撑功能失败
- P3：体验或文档问题

5. 面试可讲功能清单：
   将真正实测通过的功能整理出来，不要把 MISSING / FAIL 的功能写成已完成。
   格式：
   - 已实测通过：
   - 部分可讲但需说明限制：
   - 暂不建议写进简历：

6. 稳定性结论：
   - 是否通过 10 分钟运行
   - 是否通过 20 轮巡检
   - 是否通过 30 分钟运行
   - 未通过则说明卡在哪里

7. 下一步建议：
   按优先级列出最多 10 条修复建议。

============================================================
十五、允许做的最小修复
============================================================

如果发现以下小问题，可以直接修：

1. launch 文件引用 YAML 路径错误
2. setup.py console_scripts 漏注册
3. shell 脚本缺少执行权限
4. 文档命令路径错误
5. topic 名称配置与 YAML 不一致
6. query/report 脚本参数名和文档不一致
7. systemd ExecStart 脚本路径错误
8. 缺少目录自动创建
9. 日志目录不存在导致脚本失败
10. 明显的 JSON 字段名不一致

修复后必须重新 build/test，并在报告中记录。

============================================================
十六、禁止做的事
============================================================

1. 不要大规模重构。
2. 不要引入 SLAM/Nav2。
3. 不要删除已有功能。
4. 不要把未实测的功能写成 PASS。
5. 不要为了通过测试而注释掉真实逻辑。
6. 不要在未确认安全的情况下让小车落地高速运动。
7. 不要硬编码我的本机绝对路径，除非项目已有约定。
8. 不要泄露 webhook token 或私密配置。
9. 不要擅自 enable systemd 开机自启，除非我明确要求。
10. 不要把硬件异常伪装成测试通过。

最后输出：
1. 你实际执行了哪些命令
2. 哪些功能 PASS / PARTIAL / FAIL / MISSING / BLOCKED
3. 你做了哪些最小修复
4. docs/SYSTEM_TEST_REPORT.md 的路径
5. 还需要我人工验证的视频/硬件动作有哪些